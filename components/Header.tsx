import React, { ReactElement } from 'react'

interface Props {
    
}

function Header({}: Props): ReactElement {
    return (
        <header className="w-full bg-[#26303d] p-2">
            -
        </header>
    )
}

export default Header
